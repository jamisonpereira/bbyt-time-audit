export const shouldOpenSummaryForStartup = (): boolean => true;

export const shouldOpenSummaryForTrayRequest = (): boolean => true;

export const shouldOpenSummaryForAppActivation = (): boolean => false;
